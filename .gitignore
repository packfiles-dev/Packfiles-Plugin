# Internal reference material — kept local, never published
# (excludes "Agent HQ plugins guide v1.0.pdf")
*.pdf

# Secrets and credentials — must never be committed (plugin auth uses OIDC)
.env
.env.*
*.pem
*.key
*.age
vault.age

# OS / editor cruft
.DS_Store
